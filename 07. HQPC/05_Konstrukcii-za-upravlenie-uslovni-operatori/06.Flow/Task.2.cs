Potato potato;
//... 
if (potato != null)
{
    if (potato.HasBeenPeeled && !potato.IsRotten)
    {
        Cook(potato);
    }
}

if (MinX <= x && x =< MaxX && MinY <= y && y <= MaxY && shouldVisitCell)
{
    VisitCell();
}